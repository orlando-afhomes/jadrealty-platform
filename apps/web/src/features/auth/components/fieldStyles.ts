import styles from './FormField.module.css';

/**
 * Shared input styling for the auth feature's text/password controls.
 * Kept in its own module so FormField stays fast-refresh-clean.
 */
export const fieldStyles = styles;
